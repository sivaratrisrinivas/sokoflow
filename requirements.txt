# Pinned for reproducible installs. Torch is CPU-capable; Docker/CI install
# the CPU wheel explicitly so a GPU is never required.
flask==3.0.3
flask-cors==5.0.1
gunicorn==23.0.0
numpy==2.1.3
torch==2.5.1
