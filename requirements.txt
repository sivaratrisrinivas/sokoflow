# CPU wheel. Docker/CI/Vercel must not pull the multi-GB CUDA torch.
--extra-index-url https://download.pytorch.org/whl/cpu
flask==3.0.3
flask-cors==5.0.1
gunicorn==23.0.0
numpy==2.1.3
torch==2.5.1+cpu
